---
import { cn } from '../../lib/utils';

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<span
	class={cn(
		'inline-flex items-center rounded-full border border-border px-2.5 py-1 text-[11px] font-semibold tracking-[0.05em] text-muted uppercase',
		className,
	)}
>
	<slot />
</span>
